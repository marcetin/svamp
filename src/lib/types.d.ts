/* 
Add custom types in here like

export type MyAwesomeType = {
	// etc
}

and access via a JSDoc comment like 

jsdoc comment syntax here -  @type {import('$lib/types').MyAwesomeType}
export let awesomeThing

*/
